# Exemplos de uso Docker utilizando docker-compose

- Exemplos de uso unico disponiveis: 
  - `glassfish`.
  - `mongodb`.
  - `postgres`.
  - `nodejs`.

- Exemplos de uso em conjunto disponiveis:
  - `glassfish` - `postgres`.
  - `postgres` - `mongodb` - `nodejs`.
